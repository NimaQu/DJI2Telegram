# 代码简化消融记录

本次以现有行为和故障清理语义为约束，逐步去掉没有实际收益的间接层；未测量性能收益。

| 阶段 | 变更 | 验证 |
| --- | --- | --- |
| 基线 | 原始工作区，无未提交改动 | 205 项测试通过 |
| 第一轮 | 删除 GatewayRuntime 未使用的数据库依赖、无人读取的 running 字段、仅转发 probe_once 的 start/reconnect 方法；重连监控直接使用具名回调，删除位置索引元组 | 205 项原有测试通过 |
| 第二轮 | 用标准库 AsyncExitStack 替换资源清理的嵌套 finally，保持关闭顺序及异常传播 | 215 项测试通过，包括新增的 10 个生命周期场景 |

生产代码净减少 27 行，未引入依赖。新增测试覆盖正常关闭、启动失败、八个资源清理步骤分别失败，检查后续清理仍按顺序执行且异常继续向上传播。测试替代了硬件访问及外部服务调用。

保留 EventBus（持久化和有界广播）、USB 独占锁、探测锁、通话协调器及硬件/第三方协议边界：这些承担具体的并发、生命周期或可测试性职责，不能仅凭“多了一层”判为多余。

验证命令：`.venv/bin/pytest -q`、`.venv/bin/ruff check src tests`、`git diff --check`。

边界：自动化测试不能替代真实 QDC507 USB 重枚举、蜂窝通话和 客户端音频联调。未进行部署。GatewayRuntime 的内部 Python 接口发生变化：构造函数不再接收 database，调用方直接使用 probe_once(reason="startup"/"reconnect")；仓库内调用已同步修改，仓库外调用方需同步适配。
